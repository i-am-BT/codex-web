# node_modules Example

Do not copy or commit the real `node_modules/` directory.

Dependencies are already declared in `package.json` and pinned by `package-lock.json`. Recreate the directory with:

```bash
npm ci
```

This keeps GitHub free of generated third-party packages, platform-specific binaries, caches, and unnecessary repository size.
